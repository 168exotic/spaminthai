# HANDOFF — 2026-09-04

> **มีการแก้ไขจากโจทย์เดิม (`CURSOR-FINAL-PROMPT.md` ลงวันที่ 2026-07-19)**
> โปรเจกต์ได้ขยับไปไกลกว่าสถานะที่โจทย์นั้นอ้างอิงเป็นอย่างมาก (ดู "ตั้งต้นผิด" ด้านล่าง)
> ไฟล์นี้จึงสะท้อน **สถานะจริงของ main HEAD ปัจจุบัน** ไม่ใช่สถานะ v1.2.0 จากโจทย์เก่า
> เพื่อให้ agent รุ่นถัดไปทำงานต่อได้โดยไม่เขียนทับของสำคัญ การแจ้งนี้ ณ 2026-09-04 ยังไม่ได้รับ
> การอนุมัติให้ลบ Worker / สร้าง tag ใด ๆ

---

## 1. เลขเวอร์ชันสด (สอบจาก endpoint จริง "live")

| รายการ | ค่าจริง | หมายเหตุ |
|---|---|---|
| Web (live `/api/version`) | **1.3.0** (เผยแพร่ 2026-07-21, channel weekly) | — โจทย์เก่าคาด 1.2.0 |
| App (live `/api/app`) | **2.0.2** (versionCode 15, releaseTag **2.0.4**) | — โจทย์เก่าคาด 1.0.15 |
| main HEAD | `c1cb9e3` (2026-09-01, "fix(ads): hide empty ad slot + remove internal dev text leak") | ช้ากว่า v1.2.0 เกือบ 2 เดือน |

- ยัง **ไม่มี** tag `web-v1.2.0` และยังไม่มีไฟล์ HANDOFF ใดใน branch ไหน (สร้างไฟล์นี้เป็นไฟล์แรก)
- Tag ที่มี: v1.0.15 … v2.0.4 (อานดรอยด์/แอป) — เป็นชื่อ tag ของแอปเป็นหลัก

## 2. env/secrets ที่ต้องคงไว้ (ยังไม่ได้ตรวจใน Cloudflare Dashboard — ไม่มี API token)

ชื่อตัวแปรที่โค้ดอ่านผ่าน `env.` (จาก `git grep` ณ HEAD) — **ค่าจริงไม่เอามาลงในไฟล์นี้**:

- `TURNSTILE_SECRET` (secret) / มี GitHub secret อ้างอิงถึง
- `IP_HASH_SALT` (secret) — โค้ดใช้แทนชื่อ `IP_SALT` ในโจทย์เก่า
- `DISCORD_WEBHOOK_URL` (secret) — โค้ดใช้แทน `DISCORD_WEBHOOK`
- `APP_REPORT_KEY` (secret) — key สำหรับ app/submit (ยังไม่เห็นโจทย์เก่าใช้ แต่โค้ด main ใช้)
- `TIP_ADMIN_PASSWORD` — admin API (Cloudflare Pages env)
- `MARKETING_CRON_SECRET` — ยืนยัน `POST /api/marketing/run`
- `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` — โพสต์อัตโนมัติ
- `INDEXNOW_KEY`, `SITE_URL`
- GitHub secrets (ใช้ใน Actions): `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_APITOKEN`, keystore/alias/password สำหรับ build APK, `MAIL_TOKEN`

> ⚠️ **ต้องตรวจใน Dashboard ก่อน deploy ต่อ:** ไม่เคยยืนยันจากภายนอกว่าทุกตัวบน main
> ถูกตั้งครบที่ Cloudflare แล้ว ตัวที่มีชื่อต่างจากเดิมใน `wrangler`/repo (`IP_SALT`→`IP_HASH_SALT`,
> `DISCORD_WEBHOOK`→`DISCORD_WEBHOOK_URL`) ควรเทียบกับของที่ตั้งอยู่จริง

## 3. KV / R2 bindings (จาก `wrangler.jsonc` ณ main HEAD)

```jsonc
"kv_namespaces": [ { "binding": "SPAM_KV", "id": "0a479fd6d75c431b8e7018caf819f7b2" } ]
"r2_buckets":    [ { "binding": "EVIDENCE_R2", "bucket_name": "spaminthai-evidence" } ]
```

- โจทย์เก่าพูดถึง `QUEUE_KV`, `RATE_KV` แยกจาก `SPAM_KV` **แต่ wrangler.jsonc ปัจจุบันมีเฉพาะ `SPAM_KV`**
  → โค้ดปัจจุบันดูจะใช้ SPAM_KV ตัวเดียว (ต้องตรวจกับโค้ดจริงถ้าจะแยก)
- `_worker.js` มีอยู่จริงที่ root (ถูก bundle รวม Pages Functions ตาม convention `main` field)

## 4. วิธี deploy / โครงสร้าง

- **Cloudflare Pages** (project `spaminthai`) — deploy ผ่าน GitHub Actions `deploy.yml`
  (`cloudflare/wrangler-action`, `pages deploy . --project-name=spaminthai`)
- `functions/` = Pages Functions → bundle เข้า `_worker.js` ผ่านฟิลด์ `main` ใน wrangler.jsonc
- ไม่มี build step ภายใน; `npm run dev` → `http://localhost:8788`
- `.assetsignore` กัน `node_modules` ถูกเสิร์ฟเป็น asset
- รายละเอียดเพิ่ม: `DEPLOY-GUIDE.md`, `AGENTS.md`, `docs/TOKEN-POLICY.md`, `docs/SECURITY.md`

## 5. ห้ามเขียนทับไฟล์เหล่านี้ (agent อื่น staged ไว้อัป v1.3.0)

ตามโจทย์เดิมยังคงใช้ได้ (โค้ดปัจจุบันยังอ้างอิง-derived):
- `manifest.webmanifest` (ไม่ใช่ `manifest.json`), `sw.js` (ถ้ามีเป็น `_worker.js` เสิร์ฟ), `blog/` ทั้งโฟลเดอร์
- `index.html` (ยกเว้นแก้ bug จริง — อย่า restyle)

## 6. ข้อกังวลที่ยังค้าง ส่งต่อ agent ถัดไป

- **ยังไม่มี tag `web-v1.2.0` / release** ของ Web ตามโจทย์เดิม เพราะ main ไปไกลแล้ว การ tag commit 1 ก.ย.
  เป็น `web-v1.2.0` จะติดฉลากผิด → ทิ้ง tag นี้หรือใช้เลขเวอร์ชันใหม่ (v1.3.0+) แทน
- **Worker `spaminthai1` / `spaminthai-web`**: ตามโจทย์เดิมสั่งให้ลบ แต่ยัง **ไม่ได้ลบ** เพราะ (ก) ไม่มี
  Cloudflare API token สิทธิ์จัดการ Worker ในเครื่อง และ (ข) ยังตรวจยืนยันไม่ได้ว่าไม่มี route ผูกไว้
  → ต้องตัดสินใจโดย BOSS กับ token ที่ถูกต้องก่อน
- **ยังไม่มีการ "reconcile main กับ deploy tree"** — ยังไม่รู้แน่ชัดว่า source ใน main ตรงกับ bundle
  ที่ live 100% หรือไม่ เพราะเข้าบัญชี Cloudflare ไม่ได้
- **endpoint พฤติกรรมต่างจากโจทย์เก่า** (ตรวจแล้ว live):
  - `/api/lookup?number=021365777` → `{"ok":true,"found":false,...}` (ไม่มีฟิลด์ carrierLabel แล้ว)
  - `/api/report` turnstile invalid → HTTP **400** `{"error":"detail_too_short"}` (โจทย์เก่าคาด 403 + ข้อความไทย)
  - `/api/admin/queue` → HTTP **200** คืนหน้า HTML (โจทย์เก่าคาด 404 เงียบ) — นี่คือความจริงต้องตรวจเองว่าสมใจ
  - `/api/config` → คืนหน้า HTML ของเว็บเพจ ไม่ใช่ JSON `{turnstileSiteKey}` (โจทย์เก่าคาด JSON)

## 7. Cursor สุดท้าย + งาน v1.3.0 (จากโจทย์เดิม)

"Next agent will build Web v1.3.0 on top of this: adds `/number/:num` programmatic SEO pages,
`sitemap.xml`, `robots.txt`, `blog/:slug` pretty URLs, PWA manifest + service worker, และ blog `/blog/1441`.
Staged files อยู่ใน `Desktop/spaminthai-fix/` บนเครื่อง BOSS แต่ MUST re-merge บน `_worker.js` ปัจจุบัน
— อย่าให้ agent อื่น overwrite security features"

หมายเหตุ: ปัจจุบัน `sitemap.xml`/`robots.txt`/blog มีอยู่ใน repo แล้วบางส่วน (มี `functions/api/sitemap.js`,
`robots.txt`, `blog/`) — ตรวจสถานะจริงก่อนสรุปว่าจะต้องเพิ่มอะไร

---

**สิทธิ์/การติดต่อ:** ใช้ `admin@spaminthai.com` เท่านั้นถ้าต้องการที่อยู่ติดต่อ
**ไม่เผยแพร่:** อีเมลส่วนตัว, ชื่อจริง, handle, เบอร์, ที่อยู่ ของเจ้าของ — ห้ามลงในโค้ด/README/commit/release นี้
