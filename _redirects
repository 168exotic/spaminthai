# Cloudflare Pages routing
# Clean URLs (/check, /download, /privacy, /terms) are handled automatically by
# Pages' built-in HTML handling; explicit `/x /x.html 200` rules here create a
# 308 redirect loop with the automatic /x.html -> /x redirect.
/download/spaminthai-latest.apk   https://api.spaminthai.com/download/apk   302
